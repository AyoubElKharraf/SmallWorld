# Frontend (Vite) — copier vers .env ou .env.local

# URL de l’API (vide = chemins relatifs /api, recommandé avec le proxy Vite en dev)
# VITE_API_URL=

# URL publique du site pour balises Open Graph et sitemap (sans slash final)
# VITE_SITE_URL=https://votredomaine.com
