# Copier vers .env à la racine pour docker compose (optionnel — les valeurs par défaut du compose suffisent en local)

MYSQL_ROOT_PASSWORD=rootsecret
MYSQL_DATABASE=smallworld
MYSQL_USER=voyageur
MYSQL_PASSWORD=voyageur
MYSQL_PORT=3306

APP_PORT=8080
JWT_SECRET=remplacez-par-une-longue-chaine-aleatoire
CORS_ORIGIN=
